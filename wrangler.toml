name = "classorbit-app"
pages_build_output_dir = ".vercel/output/static"

compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Run: wrangler d1 create classorbit-db
# Then replace PLACEHOLDER below with the returned database_id
[vars]
ADMIN_EMAILS = "probob.in.git@gmail.com,bobinjacob46@gmail.com"
# Razorpay publishable key id (public by design — inlined into the browser bundle at build time).
# Swap to the rzp_live_... id when going to production.
NEXT_PUBLIC_RAZORPAY_KEY_ID = "rzp_test_SzAuC3qxxEhMHT"

[[d1_databases]]
binding = "DB"
database_name = "classorbit-db"
database_id = "f2885e0a-53d0-4a1f-ad38-b50cd89b4948"
